f"Could you generate a {plot_vars} that plots {x_vars} versus {y_vars}"
f"Please make a {plot_vars} with {x_vars} on the xaxis and {y_vars} on the yaxis"
f"I need a {plot_vars} depicting the relationship between {x_vars} and {y_vars}"
f"Can you plot {x_vars} and {y_vars} on a {plot_vars}"
f"Create a graph with {x_vars} as the horizontal axis and {y_vars} as the vertical axis in the form of a {plot_vars}"
f"Could you produce a {plot_vars} comparing {x_vars} and {y_vars}"
f"Please create a {plot_vars} charting {x_vars} against {y_vars}"
f"Id like a {plot_vars} with {x_vars} and {y_vars}"
f"Generate a {plot_vars} mapping {x_vars} to {y_vars}"
f"Construct a {plot_vars} with {x_vars} versus {y_vars}"
f"I need a {plot_vars} to show {x_vars} relative to {y_vars}"
f"Plot a {plot_vars} where {x_vars} is on the xaxis and {y_vars} is on the yaxis"
f"Draw a {plot_vars} that displays {x_vars} against {y_vars}"
f"Make a {plot_vars} illustrating {x_vars} versus {y_vars}"
f"Can you make a {plot_vars} plotting {x_vars} against {y_vars}"
f"Create a {plot_vars} graph that features {x_vars} and {y_vars}"
f"Show me a {plot_vars} of {x_vars} in relation to {y_vars}"
f"Id like to see a {plot_vars} comparing {x_vars} to {y_vars}"
f"Produce a {plot_vars} that maps {x_vars} against {y_vars}"
f"Plot a {plot_vars} with {x_vars} and {y_vars} as the variables"
f"Draw a {plot_vars} where {x_vars} is compared to {y_vars}"
f"Craft a {plot_vars} diagram charting {x_vars} versus {y_vars}"
f"Design a {plot_vars} that graphs {x_vars} against {y_vars}"
f"Can you illustrate a {plot_vars} of {x_vars} versus {y_vars}"
f"Prepare a {plot_vars} showing {x_vars} relative to {y_vars}"
f"Create a {plot_vars} plotting the values of {x_vars} and {y_vars}"
f"Make a {plot_vars} that juxtaposes {x_vars} and {y_vars}"
f"Produce a {plot_vars} that sets {x_vars} against {y_vars}"
f"Generate a {plot_vars} that places {x_vars} versus {y_vars}"
f"Show {x_vars} and {y_vars} on a {plot_vars}"
f"Construct a {plot_vars} with {x_vars} in relation to {y_vars}"
f"Create a {plot_vars} that compares the values of {x_vars} and {y_vars}"
f"Id like a {plot_vars} displaying {x_vars} versus {y_vars}"
f"Make a {plot_vars} to illustrate the relationship between {x_vars} and {y_vars}"
f"Draw a {plot_vars} that shows {x_vars} and {y_vars}"
f"Please prepare a {plot_vars} of {x_vars} against {y_vars}"
f"Plot {x_vars} and {y_vars} on a {plot_vars}"
f"Create a {plot_vars} where {x_vars} is plotted against {y_vars}"
f"Id like to see {x_vars} versus {y_vars} on a {plot_vars}"
f"Show the relationship between {x_vars} and {y_vars} in a {plot_vars}"
f"Generate a {plot_vars} that charts {x_vars} versus {y_vars}"
f"Please create a {plot_vars} depicting {x_vars} and {y_vars}"
f"Make a {plot_vars} with {x_vars} against {y_vars}"
f"Design a {plot_vars} showing {x_vars} and {y_vars}"
f"Produce a {plot_vars} graphing {x_vars} and {y_vars}"
f"Id like a {plot_vars} with {x_vars} plotted against {y_vars}"
f"Create a {plot_vars} where {x_vars} is on the horizontal axis and {y_vars} on the vertical axis"
f"Please generate a {plot_vars} showing {x_vars} against {y_vars}"
f"Draw a {plot_vars} that plots {x_vars} and {y_vars}"
f"Make a {plot_vars} displaying {x_vars} versus {y_vars}"
f"Construct a {plot_vars} to illustrate {x_vars} against {y_vars}"
f"Id like a {plot_vars} mapping {x_vars} and {y_vars}"
f"Please produce a {plot_vars} comparing {x_vars} to {y_vars}"
f"Generate a {plot_vars} depicting the relationship between {x_vars} and {y_vars}"
f"Create a {plot_vars} that shows the correlation between {x_vars} and {y_vars}"
f"Plot {x_vars} versus {y_vars} on a {plot_vars}"
f"Make a {plot_vars} where {x_vars} is on the xaxis and {y_vars} on the yaxis"
f"Draw a {plot_vars} that demonstrates {x_vars} against {y_vars}"
f"Design a {plot_vars} with {x_vars} versus {y_vars}"
f"Create a {plot_vars} graph with {x_vars} and {y_vars}"
f"Please make a {plot_vars} plotting the values of {x_vars} and {y_vars}"
f"Id like a {plot_vars} to show {x_vars} versus {y_vars}"
f"Produce a {plot_vars} diagram of {x_vars} against {y_vars}"
f"Generate a {plot_vars} with {x_vars} and {y_vars}"
f"Create a {plot_vars} showing the relationship between {x_vars} and {y_vars}"
f"Make a {plot_vars} graph that maps {x_vars} against {y_vars}"
f"Please draw a {plot_vars} with {x_vars} and {y_vars}"
f"Construct a {plot_vars} depicting {x_vars} against {y_vars}"
f"Id like a {plot_vars} that charts {x_vars} versus {y_vars}"
f"Create a {plot_vars} that illustrates {x_vars} against {y_vars}"
f"Generate a {plot_vars} to plot {x_vars} versus {y_vars}"
f"Draw a {plot_vars} mapping {x_vars} against {y_vars}"
f"Make a {plot_vars} where {x_vars} and {y_vars} are compared"
f"Produce a {plot_vars} to display {x_vars} and {y_vars}"
f"Create a {plot_vars} graph where {x_vars} is plotted against {y_vars}"
f"Please design a {plot_vars} showing {x_vars} and {y_vars}"
f"Plot {x_vars} and {y_vars} on a {plot_vars} chart"
f"Generate a {plot_vars} to show {x_vars} versus {y_vars}"
f"Construct a {plot_vars} illustrating {x_vars} and {y_vars}"
f"Id like a {plot_vars} with {x_vars} and {y_vars} values"
f"Make a {plot_vars} graphing {x_vars} versus {y_vars}"
f"Please produce a {plot_vars} charting {x_vars} and {y_vars}"
f"Create a {plot_vars} with {x_vars} and {y_vars} axes"
f"Draw a {plot_vars} showing {x_vars} against {y_vars}"
f"Design a {plot_vars} comparing {x_vars} and {y_vars}"
f"Id like a {plot_vars} plotting {x_vars} and {y_vars}"
f"Produce a {plot_vars} to map {x_vars} against {y_vars}"
f"Make a {plot_vars} to display {x_vars} versus {y_vars}"
f"Create a {plot_vars} that features {x_vars} and {y_vars}"
f"Generate a {plot_vars} showing {x_vars} in relation to {y_vars}"
f"Please construct a {plot_vars} of {x_vars} and {y_vars}"
f"Id like a {plot_vars} graphing {x_vars} and {y_vars}"
f"Draw a {plot_vars} charting {x_vars} against {y_vars}"
f"Make a {plot_vars} depicting {x_vars} versus {y_vars}"
f"Produce a {plot_vars} diagram showing {x_vars} and {y_vars}"
f"Create a {plot_vars} to illustrate the relationship between {x_vars} and {y_vars}"
f"Generate a {plot_vars} mapping the values of {x_vars} and {y_vars}"
f"Design a {plot_vars} plotting {x_vars} against {y_vars}"
f"Make a {plot_vars} that shows {x_vars} versus {y_vars}"
f"Please draw a {plot_vars} that charts {x_vars} and {y_vars}"
f"Construct a {plot_vars} comparing the values of {x_vars} and {y_vars}"
f"Produce a {plot_vars} that features {x_vars} versus {y_vars}"
f"Create a {plot_vars} where {x_vars} is on the xaxis and {y_vars} on the yaxis"
f"Generate a {plot_vars} graph showing {x_vars} and {y_vars}"
f"Id like a {plot_vars} to map {x_vars} against {y_vars}"